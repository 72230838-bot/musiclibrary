name: music_library
description: A Flutter music library app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  audioplayers: ^5.2.0   # مكتبة الصوتيات

dev_dependencies:
  flutter_test:
    sdk: flutter
